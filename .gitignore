# окружение
.venv/
__pycache__/
*.py[cod]
*.egg-info/

# машинно-специфичный конфиг (создаётся при первом запуске из config.py)
config.json

# скачанные модели (model_store кладёт сюда; в .exe — в %APPDATA%\Reku\models)
models/

# скачанный движок whisper.cpp (whisper_cpp.ensure_engine; в проде — %APPDATA%\Reku\engines)
engines/

# бенч (личные записи голоса) и кэш компиляции OpenVINO
bench_audio/
ov_cache/

# артефакты
_preview_*.png
*.log
.DS_Store
Thumbs.db

# сборка .exe (PyInstaller) + инсталлятор (Inno Setup): тяжёлые, генерируются
build/
dist/
installer/
/app.ico
!packaging/app.ico
selftest.json
check.png
requirements.effective.txt

# служебные каталоги тулз
.superpowers/
.pytest_cache/

# локальный архив внутренних планов/спеков (в публичный репозиторий не идёт)
docs-archive/

# IDE
.vscode/
.idea/
