# whisper_ptt — рабочие версии (GPU-инференс на RTX 3050, CUDA 12 / cuDNN 9).
# Точные пины снимем через `pip freeze` после установки — это верхнеуровневый набор.
faster-whisper==1.2.1
ctranslate2==4.7.2

# CUDA-библиотеки (pip-пакеты nvidia-*). DLL кладутся в site-packages/nvidia/<lib>/bin,
# dictate.py добавляет эти каталоги в PATH до импорта faster_whisper.
# На машинах без NVIDIA (напр. ноут Honor) можно не ставить: grep -v '^nvidia-'.
nvidia-cublas-cu12==12.9.2.10
nvidia-cuda-nvrtc-cu12==12.9.86
nvidia-cudnn-cu12==9.23.0.39

# Intel iGPU/NPU-путь (OpenVINOBackend): GenAI-пайплайн + рантайм.
# На чисто-NVIDIA машинах безвреден (авто-детект выберет CUDA раньше).
openvino-genai==2026.2.1.0

numpy==2.2.4
sounddevice==0.5.5
pynput==1.8.2
pyperclip==1.11.0

# десктопный UI (gui.py) — основной интерфейс
PySide6==6.11.1

# legacy: старый трей на pystray (tray.py) — заменён на gui.py, можно удалить
pystray==0.19.5
Pillow
