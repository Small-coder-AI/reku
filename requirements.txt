# Reku — верхнеуровневые пины зависимостей. Полный срез окружения — requirements.lock.txt.
faster-whisper==1.2.1
ctranslate2==4.7.2

# CUDA-библиотеки (pip-пакеты nvidia-*). DLL кладутся в site-packages/nvidia/<lib>/bin,
# cuda_setup.py добавляет эти каталоги в PATH до импорта faster_whisper.
# На машинах без NVIDIA можно не ставить: grep -v '^nvidia-'.
nvidia-cublas-cu12==12.9.2.10
nvidia-cuda-nvrtc-cu12==12.9.86
nvidia-cudnn-cu12==9.23.0.39

# Intel iGPU/NPU-путь (OpenVINOBackend): GenAI-пайплайн + рантайм.
# На чисто-NVIDIA машинах безвреден (авто-детект выберет CUDA раньше).
openvino-genai==2026.2.1.0

numpy==2.2.4
sounddevice==0.5.5
pynput==1.8.2
pyperclip==1.11.0

# десктопный UI (gui.py) — основной интерфейс
PySide6==6.11.1

# только для сборки иконки (make_ico.py пишет .ico через PIL)
Pillow==12.2.0
